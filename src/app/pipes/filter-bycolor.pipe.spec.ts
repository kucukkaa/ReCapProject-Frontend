import { FilterBycolorPipe } from './filter-bycolor.pipe';

describe('FilterBycolorPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBycolorPipe();
    expect(pipe).toBeTruthy();
  });
});
